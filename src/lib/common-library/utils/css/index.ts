/**
 * CSS Utilities
 *
 * Custom CSS files for gradients and other visual effects.
 * Import CSS files directly where needed:
 *
 * @example
 * // In app.css or component styles
 * @import "$lib/common-library/utils/css/gradients.css";
 */

// Note: CSS files are imported directly, not re-exported through JS
